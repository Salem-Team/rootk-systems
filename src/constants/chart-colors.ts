/** Dashboard / report series colors — theme-aware via CSS vars. */
export const CHART = {
  present: "var(--chart-1)",
  wfh: "var(--chart-2)",
  late: "var(--chart-3)",
  absent: "var(--chart-4)",
  rate: "var(--primary)",
  hours: "var(--chart-1)",
  accent: "var(--chart-5)",
} as const;
