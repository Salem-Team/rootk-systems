/** Default tenant — maps to future Prisma `Company.id`. */
export const DEFAULT_COMPANY_ID = "cmp_rootk_001";

/** Storage namespace for this product surface (HR). Swap to rootk.crm.* for CRM. */
export const STORAGE_NAMESPACE = "rootk.hr";

/** Bump when seed shape changes to force demo reset. */
export const SEED_VERSION = 14;
